<!-- Footer -->
<footer class="text-center">
    {% if pages.index.meta.address or pages.index.meta.social or pages.index.meta.footer %}
        <div class="footer-above">
            <div class="container">
                <div class="row">
                    {% if pages.index.meta.address %}
                        <div class="footer-col col-md-4">
                            <h3>{{ pages.index.meta.address.title }}</h3>
                            <p>{{ pages.index.meta.address.street }}
                                <br>{{ pages.index.meta.address.city }}, {{ pages.index.meta.address.state }} {{ pages.index.meta.address.zip }}, {{ pages.index.meta.address.country }} </p>
                        </div>
                    {% endif %}
                    {% if pages.index.meta.social %}
                        <div class="footer-col col-md-4">
                            <h3>Social & Web</h3>
                            <ul class="list-inline">
                                {% set social_urls = {
                                    'facebook': 'https://www.facebook.com/',
                                    'github': 'https://github.com/',
                                    'google-plus': 'https://plus.google.com/+',
                                    'instagram': 'https://www.instagram.com/',
                                    'twitter': 'https://twitter.com/'
                                } %}
                                {% for site,social in pages.index.meta.social %}
                                    <li>
                                        <a target="_blank" href="{% if site != 'skype' %}{{ social_urls[site] }}{{ social }}{% else %}skype:{{ social }}?call{% endif %}" class="btn-social btn-outline"><i class="fa fa-fw fa-{{ site }}"></i></a>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    {% endif %}
                    {% if pages.index.meta.footer %}
                        <div class="footer-col col-md-4">
                            <h3>{{ pages.index.meta.footer.heading }}</h3>
                            <p>{{ pages.index.meta.footer.paragraph }}</p>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    {% endif %}
    <div class="footer-below">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                   Developed by {{ site_title }} &#9889; {{ pages.index.meta.date | date('Y') }}
                </div>
                <br><br>
                <div class="col-lg-12">
                    All the content of this website is informative and non-commercial, 
                    does not imply a commitment to develop, launch or schedule delivery 
                    of any feature or functionality, should not rely on it in making decisions, 
                    incorporate or take it as a reference in a contract or academic 
                    matters. Likewise, the use, distribution and reproduction by any means, 
                    in whole or in part, without the authorization of the author and / or 
                    third-party copyright holders, as applicable, is prohibited.
                </div>
            </div>
        </div>
    </div>
</footer>
<!-- Scroll to Top Button (Only visible on small and extra-small screen sizes) -->
<div class="scroll-top page-scroll hidden-sm hidden-xs hidden-lg hidden-md">
    <a class="btn btn-primary" href="#page-top">
        <i class="fa fa-chevron-up"></i>
    </a>
</div>
